// agenteval is a standalone, dependency-free Go framework for
// behaviorally regression-testing LLM agents (design: T&A Flow docs/30).
// It intentionally imports NO business logic — it is extractable to its
// own repository unchanged. See README.md.
module github.com/mdilshan/agenteval

go 1.25.0
